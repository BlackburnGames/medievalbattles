# Warnings present before the port began. Only ever remove lines.
# Regenerate with: MB_WRITE_BASELINE=1 php tests/smoke.php
#
# Was 21 entries. The mysqli conversion retired 20 of them: they were all
# mysql_result() rejecting a field name passed in the row position, which
# mb_db_result() accepts by design. The survivor is not a port artifact -- the
# guild listing selects guild.flag, a column that has never existed in db.sql,
# so the query has always returned false. Fixing the query is Phase 3 work
# because it changes what the page renders.
gc.php:91 | Warning: mysqli_fetch_array() expects parameter 1 to be mysqli_result, boolean given
