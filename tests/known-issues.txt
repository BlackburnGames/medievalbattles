# Warnings present before the port began. Only ever remove lines.
# Regenerate with: MB_WRITE_BASELINE=1 php tests/smoke.php
#
# Empty as of the PHP 8 port. It held 21 entries at the end of Phase 1: 20 were
# mysql_result() rejecting a field name passed in the row position, retired by
# the move to mb_db_result(), and the last was the guild listing selecting a
# column that has never existed in db.sql, fixed in gc.php because on PHP 8 it
# escalated from a warning to a fatal TypeError.
