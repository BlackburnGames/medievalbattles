# Unescaped output audit baseline -- see tests/xss-audit.php
# Player data, request or stored, that reaches an echo unescaped, as
# file, variable and the worst HTML context it lands in. This list may only
# ever SHRINK. Regenerate with: php tests/xss-audit.php --stored --accept

